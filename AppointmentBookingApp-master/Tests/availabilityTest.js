const request = require('supertest');
const app = require('../app');

describe('Availability Endpoints', () => {
  test('should pass initial sanity check',  () => {
    expect(true).toBe(true);
  });
});